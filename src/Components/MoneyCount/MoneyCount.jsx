import React, { useState } from "react";

export default function MoneyCount({ money }) {
  return (
    <div>
      <div>
        <p>Money: ${money}</p>
        <p></p>
      </div>
    </div>
  );
}
